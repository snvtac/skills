interface:
  display_name: "Linear Manager"
  short_description: "HTTP-only Linear manager (no Linear MCP) for tickets, templates, status, and comments"
  default_prompt: "Use this skill only via HTTP GraphQL with python3 scripts/linear_manager.py. Create/read Linear tickets and sub-tickets, list issue templates, optionally create tickets with --template-id/--template-name/--use-default-template, update issue status/fields, and read/create comments. Never use mcp__linear__* tools. Write commands (create/update/delete/comment) are dry-run by default and require --execute for real mutation. If token is missing or request fails, return error with short suggestions and do not fallback to MCP."
